export const parseInput = (input) => {
  // expected input: 1 AUD to USD
  // TODO: parse or tokenize the input string

  return {
    fromAmount: '',
    fromCurrency: '',
    toCurrency: '',
  };
};
